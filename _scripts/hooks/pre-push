#!/usr/bin/env bash
# _scripts/hooks/pre-push — git push 前自动运行测试套件
# 由 bootstrap.sh 通过 `git config core.hooksPath _scripts/hooks` 启用。

set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"

# make test 会统一处理 shellcheck 缺失的情况（提示跳过 shellcheck，但其余测试照常运行）
make -C "$ROOT" test
